'use client';

import PlantillaFormPage from '../[id]/page';

// This component is intentionally left simple.
// The logic is handled in the dynamic route page `../[id]/page.tsx`.
export default function NuevaPlantillaPage() {
    return <PlantillaFormPage />;
}
